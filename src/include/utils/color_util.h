#pragma once

#include "util.h"

static const auto COL_RED = Color3(.65, .05, .05);
static const auto COL_GREEN = Color3(.12, .45, .15);
static const auto COL_BLUE = Color3(.1, .2, .5);
static const auto COL_WHITE = Color3(.73, .73, .73);
static const auto COL_LIGHT = Color3(35, 35, 35);
static const auto COL_ZERO = Color3(0, 0, 0);
